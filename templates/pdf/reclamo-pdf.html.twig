<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Reclamo #{{ reclamo.id }}</title>

    <style>

        @page {
            size: 80mm 150mm;
            margin: 3mm;
        }

        body {
            font-family: DejaVu Sans, sans-serif;
            font-size: 20px;
            margin: 0;
            padding: 0;
            width: 150mm;
            height: 80mm;
            transform: rotate(90deg);
            transform-origin: left top;
        }

        .ticket {
            width: 95%;
            text-align: left;
            box-sizing: border-box;
            margin: 0 auto;
        }

        .header {
            text-align: center;
            margin-bottom: 6px;
            border-bottom: 1px solid #000;
            padding-bottom: 3px;
        }

        .header h1 {
            font-size: 16px;
            margin: 0;
        }

        .header p {
            font-size: 12px;
            margin: 2px 0 0 0;
        }

        .section {
            margin-bottom: 5px;
            line-height: 1.3;
        }

        .label {
            font-weight: bold;
        }

        .badge {
            display: inline-block;
            padding: 2px 6px;
            border-radius: 3px;
            font-size: 11px;
            color: #fff;
        }

        .badge-warning { background-color: #ffc107; }
        .badge-success { background-color: #28a745; }
        .badge-info { background-color: #17a2b8; }
        .badge-secondary { background-color: #6c757d; }
    </style>
</head>
<body>
<div class="ticket">
    <div class="header">
        <h1>Reclamo #{{ reclamo.id }}</h1>
        <p>Sistema de Gestión de Reclamos</p>
    </div>

    <div class="section">
        <div><span class="label">Servicio:</span> {{ reclamo.Servicio }}</div>
        <div><span class="label">N° Cliente:</span> {{ reclamo.numeroCliente }}</div>
        <div><span class="label">N° Medidor:</span> {{ reclamo.numeroMedidor }}</div>
        <div><span class="label">Domicilio:</span> {{ reclamo.Domicilio }}</div>
        <div><span class="label">Usuario:</span> {{ reclamo.Usuario }}</div>
        <div><span class="label">Motivo:</span> {{ reclamo.Motivo }}</div>
    </div>

    <div class="section">
        <div class="label">Detalle:</div>
        <div>{{ reclamo.Detalle|default('Sin detalle') }}</div>
    </div>

    <div class="section">
        <div>
            <span class="label">Estado:</span>
            <span class="badge badge-{{
            reclamo.estado == 'Impreso' ? 'warning' :
            (reclamo.estado == 'Atendiendo' ? 'success' :
            (reclamo.estado == 'En Guardia' ? 'info' : 'secondary'))
            }}">{{ reclamo.estado }}</span>
        </div>
        <div><span class="label">Fecha de creación:</span> {{ reclamo.fechaCreacion|date('d/m/Y H:i') }}</div>
    </div>
</div>
</body>
</html>


